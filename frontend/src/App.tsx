import { AppInitializer } from '@/components';
import Chatroom from '@/components/Chatroom.temp';

const App = () => {
  return (
    <AppInitializer>
      <Chatroom />
    </AppInitializer>
  );
};

export default App;
